import { Button, Card, CardActions, CardContent, CardMedia, Input, TextField, Typography, makeStyles } from '@mui/material'
import { signOut } from 'firebase/auth'
import React from 'react'
import { auth } from '../../fireBaseConfig'
import { useNavigate } from 'react-router-dom'

export default function ImageUpdateCard() {
    const navigate = useNavigate()
    
    return (
        <div style={{}}>
         
        </div>
    )
}
